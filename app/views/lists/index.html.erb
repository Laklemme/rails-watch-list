<h1>All lists</h1>
<ul>
  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list) %>
    <% if list.photo.attached? %>
    <%= cl_image_tag list.photo.key, height: 80, width: 100, crop: :fill %>
    <% end %>
  <% end %>
</ul>

<%= link_to 'Create a new list', new_list_path %>
